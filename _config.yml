title: SonarQube Docker Deployment
description: Production-grade SonarQube Community Build with Docker Compose, multi-environment support, and automated dependency management
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
show_downloads: false

kramdown:
  syntax_highlighter_opts:
    block:
      disable: true
